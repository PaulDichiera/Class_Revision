#include <iostream>
#include <memory>

#include "BST.h"

BST::BST(){
    // sets the root of the tree?
}

BST::~BST(){
    //calls clear()
    //clear();
}

void BST::clear(){
    //cycles through the tree to clear the nodes
}

bool BST::contains(const int data) const {
    //get function to retrieve data from node
}

void BST::add(const int data){
    //create new node in memory using a pointer to the node, filter left or right. apply pointer and positions.
    //uses left and right pointer instead of ->next
}